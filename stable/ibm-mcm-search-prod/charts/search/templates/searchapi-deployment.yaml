# Licensed Materials - Property of IBM
# 5737-E67
# (C) Copyright IBM Corporation 2016, 2019 All Rights Reserved
# US Government Users Restricted Rights - Use, duplication or disclosure restricted by GSA ADP Schedule Contract with IBM Corp.

apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ template "search.fullname" . }}-searchapi
  labels:
    app: {{ template "search.name" . }}
    chart: {{ template "search.chart" . }}
    component: "searchapi"
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ template "search.name" . }}
      component: "searchapi"
      release: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app: {{ template "search.name" . }}
        component: "searchapi"
        release: {{ .Release.Name }}
        chart: {{ template "search.chart" . }}
        heritage: {{ .Release.Service }}
      annotations:
        productName: "IBM Multicloud Manager - Server"
        productID: "317a72a4f22645aaa1b7a07a83c60af5"
        productVersion: "3.1.1"
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: beta.kubernetes.io/arch
                operator: In
                values:
                {{- range .Values.global.arch }}
                - {{ . }}
                {{- end }}
      tolerations:
        - key: dedicated
          operator: Exists
          effect: NoSchedule
      containers:
      - name: search-api
        image: "{{ .Values.searchapi.image.repository }}:{{ .Values.searchapi.image.tag }}"
        imagePullPolicy: {{ .Values.searchapi.image.pullPolicy }}
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            add:
            - NET_BIND_SERVICE
            drop:
            - ALL
          privileged: false
          readOnlyRootFilesystem: false
        env:
        - name: ICP_STANDALONE
          value: "{{ .Values.global.standalone }}"
        - name: cfcRouterUrl
          value: {{ .Values.global.cfcRouterUrl }}
        - name: PLATFORM_IDENTITY_PROVIDER_URL
          value: {{ .Values.global.cfcRouterUrl }}/idprovider
        - name: redisEndpoint
          value: '{{ template "search.fullname" . }}-search-redisgraph:6379'
        - name: redisSSLEndpoint
          value: '{{ template "search.fullname" . }}-search-redisgraph:6380'
        - name: redisPassword
          valueFrom:
            secretKeyRef:
              name: {{ .Release.Name }}-redisgraph-user-secrets
              key: redispwd
        ports:
        - containerPort: 4010
          protocol: TCP
        livenessProbe:
          failureThreshold: 3
          httpGet:
            path: /livenessProbe
            port: 4010
            scheme: HTTPS
          initialDelaySeconds: 30
          periodSeconds: 30
          successThreshold: 1
          timeoutSeconds: 1
        readinessProbe:
          failureThreshold: 3
          httpGet:
            path: /readinessProbe
            port: 4010
            scheme: HTTPS
          initialDelaySeconds: 5
          periodSeconds: 10
          successThreshold: 1
          timeoutSeconds: 1
        volumeMounts:
        - name: searchapi-certs
          mountPath: /opt/ibm/search-api/sslcert
        - name: mcm-search-certs
          mountPath: /opt/secrets
        - name: redis-certs
          mountPath: /opt/ibm/search-api/rediscert/redis.crt
          subPath: redis.crt
      {{- if .Values.global.pullSecret }}
      imagePullSecrets:
      - name: {{ .Values.global.pullSecret }}
      {{- end }}
      volumes:
      - name: searchapi-certs
        secret:
          secretName: {{ template "search.fullname" . }}-searchapi-secrets
          items:
          - key: ca.crt
            path: searchapica.crt
          - key: tls.crt
            path: searchapi.crt
          - key: tls.key
            path: searchapi.key
      - name: redis-certs
        secret:
          secretName: {{ .Release.Name }}-redisgraph-secrets
          items:
          - key: ca.crt
            path: redis.crt
          - key: tls.crt
            path: server.crt
          - key: tls.key
            path: server.key
      - name: mcm-search-certs
        secret:
          secretName: {{ .Release.Name }}-search-secrets
      {{- if .Values.global.nodeSelector.enabled }}
      nodeSelector:
      {{- if .Values.global.nodeSelector.os }}
        "beta.kubernetes.io/os" : {{ .Values.global.nodeSelector.os }}
      {{- end }}
      {{- if .Values.global.nodeSelector.customLabelSelector }}
        {{ .Values.global.nodeSelector.customLabelSelector }} : {{ .Values.global.nodeSelector.customLabelValue}}
      {{- end }}
      {{- end }}
