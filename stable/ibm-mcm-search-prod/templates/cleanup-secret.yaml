# Licensed Materials - Property of IBM
# 5737-E67
# (C) Copyright IBM Corporation 2016, 2019 All Rights Reserved
# US Government Users Restricted Rights - Use, duplication or disclosure restricted by GSA ADP Schedule Contract with IBM Corp.

apiVersion: batch/v1
kind: Job
metadata:
  name: {{ template "ibm-mcm-search.fullname" . }}-cleanup-secret
  namespace: {{ .Release.Namespace }}
  annotations:
    "helm.sh/hook": post-delete
    "helm.sh/hook-delete-policy": hook-succeeded
  labels:
    app: {{ template "ibm-mcm-search.fullname" . }}
    chart: {{ template "ibm-mcm-search.chart" . }}
    heritage: {{ .Release.Service }}
    release: {{ .Release.Name }}
spec:
  template:
    metadata:
      name: {{ template "ibm-mcm-search.fullname" . }}-cleanup-secret
      labels:
        app: {{ template "ibm-mcm-search.fullname" . }}
        chart: {{ template "ibm-mcm-search.chart" . }}
        heritage: {{ .Release.Service }}
        release: {{ .Release.Name }}
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: beta.kubernetes.io/arch
                operator: In
                values:
                {{- range .Values.global.arch }}
                - {{ . }}
                {{- end }}
      tolerations:
        - key: dedicated
          operator: Exists
          effect: NoSchedule
      containers:
        - name: kubectl
          image: "{{ .Values.kubectl.repository }}:{{ .Values.kubectl.tag }}"
          imagePullPolicy: "{{ .Values.kubectl.pullPolicy }}"
          securityContext:
            privileged: false
            readOnlyRootFilesystem: false
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            runAsUser: 1000
            capabilities:
              drop:
              - ALL
          command:
          - kubectl
          - delete
          - secret
          - -l
          - 'certmanager.k8s.io/certificate-name in ({{ .Release.Name }}-search-redis-ca-cert, {{ .Release.Name }}-search-aggregator-ca-cert)'
          - -n
          - {{ .Release.Namespace }}
      restartPolicy: OnFailure
      {{- if .Values.global.pullSecret }}
      imagePullSecrets:
      - name: {{ .Values.global.pullSecret }}
      {{- end }}
