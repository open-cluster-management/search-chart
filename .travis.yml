---
dist: xenial
sudo: required
language: generic

   
install:
  - docker info

os:
  - linux

services:
  - docker

branches:
  only:
    - master
    - /^release-[0-9]+\..*$/


before_script:
  - make tool
  - make init
  - make lint

script:
  - make build
